def prompt_guess(position, length):
    guess = input('Now guess 2 letters corresponding to letters 1 to 2 of the unknown word:')
    position = 0
    length = 2
    if len(guess) != length:
        print('Invalid')
    else:
        print('Valid guess')

    guess = input('Now guess 3 letters corresponding to letters 1 to 3 of the unknown word:')
    position = 0
    length = 3
    if len(guess) != length:
        print('Invalid')
    else:
        print('Valid guess')

def prompt_guess(position, length)
    position = 0
    for i in range(2, length)
        guess = input('Now guess'+i+'letters corresonding to letters
                      
